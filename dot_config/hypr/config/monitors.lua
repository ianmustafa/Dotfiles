-- ── Monitors ─────────────────────────────────────────────────────
-- Run `hyprctl monitors` to confirm output names.
-- Ported from niri/cfg/display.kdl

hl.monitor({
    name    = MONITOR1,
    mode    = "2560x1080@60",
    pos     = { 0, 0 },
    scale   = 1,
})
