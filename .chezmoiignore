# Tmux plugins (managed by tpm, not chezmoi)
.config/tmux/plugins

# Neovim syncthing artifact
.config/nvim/.stfolder

# Neovim lazy-lock (changes frequently, machine-specific)
.config/nvim/lazy-lock.json

# Docker runtime data
Docker/data
Docker/logs
Docker/build

# Ghostty noctalia theme (generated by noctalia on bg change)
.config/ghostty/themes/noctalia

# Zsh history
.zsh_history
